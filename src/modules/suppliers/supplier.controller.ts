import { Request, Response } from "express";

export class SupplierController{
    static async createSupplier(req: Request, res: Response){

    }

    static async getOrCreateSupplier(req: Request, res: Response){
        
    }
}